% ============================================================================
%  SUPPLEMENTARY MATERIAL -- uploaded as a separate document.
%
%  Figure S1 was Figure 1 of the manuscript. It is here because the article is held to ten
%  published pages and this figure occupies one of them: it is drawn at 175 x 240 mm and
%  \fitfloat scales it to the full text height, so it cannot be shortened without dropping
%  the type below legibility. Every other float was tested and none changes the page count.
%
%  The main text refers to it as Fig. S1 at each of the four steps it depicts.
% ============================================================================
\documentclass[12pt]{article}
% PORTRAIT, narrow margins. The drawing is 175 x 240 mm: portrait gives it about 250 mm
% of text height, so it sets at full size here rather than being scaled to fit a column.
\usepackage[margin=0.5in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds}

% Carried over from the manuscript preamble, unchanged: the figure calls both, and a
% re-implementation here would be a second version of them free to drift.
\newsavebox{\fitbox}
\newsavebox{\capbox}
\newcommand{\maxwidth}[1]{%
  \sbox{\fitbox}{#1}%
  \ifdim\wd\fitbox>\linewidth
    \resizebox{\linewidth}{!}{\usebox{\fitbox}}%
  \else
    \usebox{\fitbox}%
  \fi
}
\newcommand{\fitfloat}[2]{%
  \sbox{\capbox}{\parbox{\linewidth}{\caption{#2}}}%
  \sbox{\fitbox}{#1}%
  \ifdim \wd \fitbox>\linewidth
    \sbox{\fitbox}{\resizebox{\linewidth}{!}{\usebox{\fitbox}}}%
  \fi
  \dimen0=\dimexpr \ht \fitbox+\dp \fitbox \relax
  \dimen2=\dimexpr \textheight-\ht \capbox-\dp \capbox-\baselineskip \relax
  \ifdim \dimen0>\dimen2
    \sbox{\fitbox}{\resizebox{!}{\dimen2}{\usebox{\fitbox}}}%
  \fi
  \usebox{\fitbox}\par \usebox{\capbox}%
}

\renewcommand{\figurename}{Figure S}
\makeatletter
\renewcommand{\thefigure}{1}
\makeatother

\begin{document}
\pagestyle{empty}

\begin{center}
{\large\bfseries Supplementary Material}\\[2pt]
CTSpinoPelvic1K: spine, pelvis, ribs and femora in one coordinate frame,
annotated for lumbosacral transitional anatomy
\end{center}

\vspace{1em}

\begin{figure}[h!]
\centering
% SINGLE DOWNWARD FLOW ON A FIXED GRID.
%
% THE RECURRING FAILURE HERE, THREE TIMES NOW: minimum height is a FLOOR, not a height.
% A box whose text wraps to one more line than expected grows downward into the row beneath
% and prints through it, and this is invisible in the source. The rule that finally works:
% EVERY BOX IS AT MOST TWO LINES. Text is kept short enough to guarantee that at the box
% width given, anything longer goes in the caption, and rows are then spaced at twice the
% two-line height. Detail hangs to the RIGHT of the version boxes where there is free width,
% not inside them where it would add lines.
%
% Also fixed:
%   * v3|v4|v5 ran ACROSS, so the arrow into v3 came up from below-left. A version history
%     is a sequence and now reads as one.
%   * the previous attempt was drawn ~200mm wide and shrunk by \maxwidth to fit, which is
%     why the type came out at 6pt in a field of white space. The design width is ~175mm
%     against a ~165mm \linewidth, so \maxwidth scales by about 0.94 and the type stays
%     essentially the size it was set in.
%   * the stub joining v5 to its description was drawn THROUGH the description.
%   * jargon: "geometric invariants" told the reader nothing. The gates now say what is
%     checked, in words a surgeon would use.
%   * "out" is a reserved TikZ key: naming a node (out) silently drops its shape and
%     surfaces much later as "missing \item". The final node is (rel).
\fitfloat{%
\begin{tikzpicture}[
  x=1mm, y=1mm,
  font=\small,
  every node/.style={inner sep=0pt},
  bx/.style={draw, rounded corners=1.5pt, align=center, text width=#1,
             minimum height=11mm, inner sep=2.5pt},
  src/.style={bx=52mm, fill=black!5},
  proc/.style={bx=168mm, fill=blue!7},
  rec/.style={bx=36mm, fill=black!3},
  ver/.style={bx=24mm, fill=green!14, minimum height=9mm},
  det/.style={align=left, text width=124mm, font=\small, anchor=west},
  gate/.style={bx=168mm, fill=orange!14},
  fin/.style={bx=168mm, fill=green!22},
  ar/.style={-{Stealth[length=5pt]}, semithick, black!70},
  ln/.style={semithick, black!70},
]

% --- row 1: sources ------------------------------------------- 2 lines, half 7
\node[src] (tcia)   at (-58, 0) {\textbf{TCIA CT Colonography}\\[2pt]CT volumes};
\node[src] (spine)  at (  0, 0) {\textbf{CTSpine1K}\\[2pt]vertebral labels};
\node[src] (pelvic) at ( 58, 0) {\textbf{CTPelvic1K}\\[2pt]sacrum + hip labels};
\draw[ln] (tcia.south) -- (-58,-15) -- (0,-15);
\draw[ln] (pelvic.south) -- (58,-15) -- (0,-15);
\draw[ln] (spine.south) -- (0,-15);
\draw[ar] (0,-15) -- (0,-22);

% --- row 2: the crosswalk ------------------------------------- centre -30, half 8
\node[proc] (cross) at (0,-30) {\textbf{match each label set to the series it was drawn
  on}\\[2pt]every patient was scanned prone \emph{and} supine; neither source published the
  series it used};

% --- row 3: the four kinds of record -------------------------- centre -56, half 9
\node[rec] (fused) at (-60,-56) {\textbf{fused}\\[2pt]both on one series\\[1pt]$n{=}342$};
\node[rec] (sep)   at (-20,-56) {\textbf{separate}\\[2pt]pelvis: other series\\[1pt]$n{=}351$};
\node[rec] (sonly) at ( 20,-56) {\textbf{spine only}\\[2pt]no pelvic label\\[1pt]$n{=}89$};
\node[rec] (ponly) at ( 60,-56) {\textbf{pelvis only}\\[2pt]held back\\[1pt]$n{=}20$};
\draw[ln] (cross.south) -- (0,-44);
\draw[ln] (-60,-44) -- (60,-44);
\foreach \x in {-60,-20,20,60} \draw[ar] (\x,-44) -- (\x,-47);

% --- row 4: corrections --------------------------------------- centre -82, half 8
\node[proc] (fix) at (0,-82) {\textbf{correct errors in the source ground
  truth}\\[2pt]split vertebrae, skipped levels, levels unlabelled inside the FOV:
  $n{=}112$ spine fixes / 103 cases, $n{=}13$ pelvic fixes / 12 cases};
\foreach \x in {-60,-20,20,60} \draw[ln] (\x,-65) -- (\x,-70);
\draw[ln] (-60,-70) -- (60,-70);
\draw[ar] (0,-70) -- (fix.north);

% --- row 5: filling in the missing half ----------------------- centre -108, half 8
\node[proc] (dense) at (0,-108) {\textbf{pseudolabel the missing half --- asymmetrically,
  by design}\\[2pt]a pelvis has no enumeration to get wrong; a spine does, so the 20
  pelvic-only spines were corrected \emph{by hand}};
\draw[ar] (fix.south) -- (dense.north);

% --- rows 6-8: the releases, in order -------------------------- 20mm apart
\node[ver] (v3) at (-72,-132) {\textbf{v3}};
\node[ver] (v4) at (-72,-152) {\textbf{v4}};
\node[ver] (v5) at (-72,-172) {\textbf{v5}};
\node[ver] (v6) at (-72,-192) {\textbf{v6}};
\draw[ar] (dense.south) -- (0,-122) -- (-72,-122) -- (v3.north);
\draw[ar] (v3.south) -- (v4.north);
\draw[ar] (v4.south) -- (v5.north);
\draw[ar] (v5.south) -- (v6.north);

\foreach \y in {-132,-152,-172,-192} \draw[ln, black!35] (-60,\y) -- (-54,\y);
\node[det] at (-52,-132) {added \textbf{femora} and the lower thoracic levels, and carved
  \textbf{S1} out of the sacrum as its own class --- the caudal anchor};
\node[det] at (-52,-152) {added \textbf{ribs}: M\"oller's binary rib net unioned with
  TotalSegmentator, numbered from TotalSegmentator, then $n{=}152$ cases to human review};
\node[det] at (-52,-172) {\textbf{relabelled lumbar ribs} to their own class rather than
  forcing them to be rib~12 ($n{=}16$ cases)};
\node[det] at (-52,-192) {added \textbf{surgical instrumentation} as its own classes rather
  than leaving it inside the bone that absorbed it ($n{=}11$ cases), renumbered 0068 against
  the twelfth rib, and corrected hip laterality ($n{=}22$ cases)};

% --- row 9: the checks ----------------------------------------- centre -216, half 8
\node[gate] (qc) at (0,-216) {\textbf{release gates, in order --- a failure stops the
  chain}\\[2pt]label shares its CT's affine and structures are correctly sided
  $\rightarrow$ each rib articulates with the vertebra its number implies $\rightarrow$
  measures fall in physiological range};
\draw[ar] (v6.south) -- (-72,-202) -- (0,-202) -- (qc.north);

% --- row 10: release ------------------------------------------- centre -240
\node[fin] (rel) at (0,-240) {\textbf{802 released records}\\[2pt]spine, pelvis, per-level
  ribs and femora in one frame; 33 carry a radiologist Castellvi grade};
\draw[ar] (qc.south) -- (rel.north);
\end{tikzpicture}}{How the dataset was built. Three public collections are combined patient by
patient. Neither annotation source published the CT series its labels were drawn on, so each
label set is matched to the series whose bone agrees with it. In 351 of those patients the
two collections had drawn their outlines on \emph{different scans of the same person}.
Numbers marked $n$ are scans, or corrections, at that step.%
\label{fig:pipeline}}
\end{figure}

\end{document}
